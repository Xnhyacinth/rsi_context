# Repair C Qwen landscape — 2026-08-17

Immutable record:
`artifacts/hard-causal-gate/qwen-repair-c-causal-replay-disposable-v1.json`

- Seed: `hard-repair-c-visible-causal-disposable-v1`
- Fingerprint: `b40c5ae1d5ce2fd7838c38afaba52d5567cff55559abb3285095dc7e16c3e3ea`
- 16 items/profile (4 per evidence-position stratum); 1472/1472 calls
- 14,607,159 input / 11,648 output tokens; 444.44 reader-seconds; `$0`
- Dense generator: node identity on dossier bindings; scores on `ds-` chunks
  without node IDs; query still lists six `nd-` IDs and forbids `dossier`/`ds-`
- Compositional query unchanged from Repair B
- **difficulty_assessment: failed**. A2 not started.
- Do not overwrite Repair A/B or this file.

Cheap dense probe that led here (different seed, 8 items, 7 conditions):
`.hl/artifacts/repair-c-probe-2026-08-17.md`.

## Kill-list vs actual

| Pre-registered kill            | Compositional                                           | Dense                                                        |
| ------------------------------ | ------------------------------------------------------- | ------------------------------------------------------------ |
| gold-only < 0.85               | **0.875** (2/16 middle `fx-`)                           | **1.0**                                                      |
| bounded-oracle < 0.80          | **0.750** (1× `fx-`, 3× `INSUFFICIENT`)                 | **1.0**                                                      |
| non-oracle ≥ 0.90              | max **0.1875** (MMR hops=1/2 relevance)                 | max **0.3125** (hops=1/2 RANK source)                        |
| lexical / hops=0 RANK saturate | lexical 0; hops=0 RANK 0                                | lexical **0**; hops=0 RANK **0** (recall 0.54)               |
| disagreement < 0.20            | **0** (top two MMR clones)                              | **0** (hops=1 vs hops=2 RANK source clones)                  |
| stratum 0 or 1                 | fail (head/tail/distributed 0 under _pooled_ strongest) | fail (head 0, distributed 0 under hops=1/2 RANK source; n=4) |

Pooled strongest is `v1:query_bm25:2:rank:none:source` (dense-oriented).
Full context 0.96875 (1 dense distributed miss) / no-context 0 / gold-drop
0.96875 / counterfactual 0.9375 / replay 0.1875 × 5, sd 0.

## What this supports

Repair C did what it was for: **dense is no longer BM25-saturated**. hops=0
RANK and lexical score 0 with incomplete gold (recall ~0.54, bindings without
scores). hops=1 RANK source reaches 0.3125 inside [0.25, 0.75] and does not
hit 0.90. Gold-only/oracle stay 1.0. Topology tests lock that split at 32K.

It did not produce an A2-ready panel:

1. Compositional bounded-oracle fell to 0.75 on this 16-item seed (Repair B
   was 0.875 on 8 items). The generator/query were not changed; misses are
   competing `fx-` and `INSUFFICIENT` with gold recall 1.0. Gold-only still
   clears 0.85.
2. Compositional PolicySpecV1 max 0.1875 is below the 0.25 floor (range
   0.1875 < 0.20).
3. Dense top two specs are hops=1 and hops=2 RANK source with identical
   item scores, so strongest-policy disagreement is 0. That is a grammar
   clone, not a scoring bug.
4. n=4 strata still hit 0 at head and distributed under RANK source: high
   competing figures enter the 8K pack without the qualification rule, and
   the reader returns a wrong `nd-`. Head truncation scores 4/4 on those
   same head items (13 gold fit in the first 16 chunks). Do not relax the
   0/1 stratum threshold after seeing this.

Do not start A2. Do not put `folio`/`vl-`/`dossier` in queries. Next work is
compositional 8K oracle stability and/or a pre-registered disagreement rule
that does not compare clone specs, not HELMET transfer.

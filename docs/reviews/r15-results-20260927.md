# R15 PEP/KEP reader evidence and qualification decision

**Decision:** the PEP 621/639 fixed-reader development block completed with
real Siflow usage, but the PEP lineage is **not a qualified independent
dependency parent**. The KEP-753 card has only synthetic contract evidence;
its paid entry remains closed. Qualified independent parent count remains
zero. No researcher-authored policy or researcher-update pretest ran.

## Frozen run identity and budget

The integration began at `main@dc76d798bfc49e58d599fc77bca8529a40cd063d`.
The PEP source checkout was detached and clean at
`6822259db9c95f02da739b3e2830a4aa1ae35134`; the KEP source checkout was
detached and clean at `13e8bb54ff7b1777d97c0f7f3cc9691c67414d4a`.
The shared development-only profile is `Qwen/Qwen3.6-27B` through the exact
`https://api.siflow.cn/model-api/chat/completions` route, with thinking off,
seed 42, temperature 0 and `max_tokens=2048`. Its loaded profile hash is
`b9e0df1d39f4c74b684661a7b7e4a714f33a7b0fd8741c3e183d1203ca21ce69`;
the provider revision is unavailable. The tracked PEP geometry
[`configs/r15_pep_fixed_reader_geometry_v1.json`](../../configs/r15_pep_fixed_reader_geometry_v1.json)
has SHA256 `c491cfb800aa3a2bdef794ca2cf4dd01780df1c9663085aaa14f7672cb720f63`.
The tracked registration and launch manifests bind its bytes, the tokenizer,
policy, producer files, endpoint, call order and budget before transport.

The PEP launch permits six complete two-session cases and at most **12 task
calls plus two in-block canaries**, with no auxiliary calls. The local
worst-path input is 18,171 task tokens; requested task output is bounded by
12 × 2,048, giving a **42,747 local-input-plus-requested-output planning
ceiling**. With two 62-input/2,048-requested-output canaries, the global
planning ceiling is **46,967 tokens and 14 HTTP attempts**. These are
admission bounds, not billed-token predictions.

## Real Siflow canaries and task block

The first standalone exact-profile canary returned the correct `amber`,
matched 62 provider/local input tokens, had the expected model echo and
`stop` finish, but failed the initial output check: provider reported 2
output tokens while the visible answer tokenized to 1. Its immutable
[artifact](/volume/pt-dev/qjiu/rsi_context/artifacts/rsi-core-v1/r15-preflight/exact-profile-canary-live-pre-20260926.json)
has SHA256 `62cdf0abe33a32880191ea601582a4141dd0831ce3005b240695dc83119c4916`.
Earlier R10/R11/R12 Qwen `amber` canaries also reported provider output 2.
The canary rule was revised in a separate commit to require **provider 2 /
visible 1**, recording the +1 difference with its token identity unknown;
it still rejects missing usage, wrong model/finish, and other output counts.
The second standalone [canary](/volume/pt-dev/qjiu/rsi_context/artifacts/rsi-core-v1/r15-preflight/exact-profile-canary-live-pre-v2-20260927.json)
passed with **62 input / 2 output** provider tokens (SHA256
`58df56c0e86215bbcd4a76651baec6855e17d3c8e6252d766fc7c16661d3cc26`).
The first failed attempt remains in the ledger.

The bounded paid PEP block is in
`/volume/pt-dev/qjiu/rsi_context_external/r15-paid-runs/pep-development-v1-20260927/`.
The private directory is mode 0700 and all seven evidence files are 0600;
none contains the Siflow credential. Key artifacts are `final.json` SHA256
`bfb321245dd761c76c44706a54b8e4a02c75e3ea80065079759369dadbf3f54c`,
`task.json` SHA256 `94ce1ce1b953e463add1b90538e37a55782087071b310448be4dcc2ac562cb29`,
and `attempts.jsonl` SHA256
`bd0983ae793cc89a9d3d5d3a5e900b3ad13a4f71457e7491b70e940e28ca676a`.
All 14 attempts have paired dispatch/response journal entries; model echoes,
finish reasons and provider usage are valid, with zero unknown-usage attempts.
The task used **18,168 input / 57 output** provider tokens; its two in-block
canaries used **124 / 4**, giving **18,292 / 61** in the block. Including the
two standalone canaries, R15 made **16 real Siflow requests** with
**18,416 input / 65 output** provider-reported tokens. Every task provider
input count equaled the registered local final-chat count. No price estimate
is asserted.

| PEP case | First-session finding | Later plan | Complete sessions |
| --- | --- | --- | --- |
| Full 621 | `license-table` | `license-table` | 2/2 passed |
| Full 639 | `license-string` | `license-string` | 2/2 passed |
| Identity-only 621 | `license-table` | `license-table` | 2/2 passed |
| Identity-only 639 | `unknown` | `license-string` | 2/2 passed |
| Source-free 621 | `unknown` | `license-string` | 1/2 passed |
| Source-free 639 | `unknown` | `license-string` | 2/2 passed |

The full-source pair is feasible. Both identity-only controls also succeed:
PEP 621 gets its legal finding without substantive rule text, and PEP 639
succeeds after an `unknown` finding by choosing the string plan. Source-free
PEP 639 also succeeds. This prevents attributing full-source success to
historical rule reading. PEP 621 and 639 are one source lineage, and this
single fixed reader run is a diagnostic, not a statistical rate estimate.

## KEP status, verification and next experiment

KEP's three constructed arms register twelve exact prompts and at most nine
worker attempts. Their aggregate local-input-plus-requested-output planning
ceiling is **62,544 tokens**. The current clean integrated
[offline artifact](/volume/pt-dev/qjiu/rsi_context/artifacts/rsi-core-v1/r15-preflight/kep-integrated-offline-v2.json)
has SHA256 `a3930ea4be4b0821c4d9e76a68bfbf8808986b5b0625591982d2ead1cb5c3b8b`:
nine **synthetic** SSE calls, 44,108/71 synthetic input/output tokens, and
`provider_usage_total=null`. The full-source scripted trajectory passed both
sessions; source-free and two-explicit-rule-neutralized trajectories passed
their first session and failed the second. The neutralization retains indirect
source cues, so these results prove wiring and byte controls only.

The final full gate was **1554 passed, 16 skipped, 80.88% branch coverage**;
[log](/volume/pt-dev/qjiu/rsi_context/artifacts/rsi-core-v1/r15-preflight/full-pytest-v2.log).
Repository Ruff and configured strict mypy passed (379 files). Whole-source
Bandit retained **81 LOW / 2 MEDIUM / 0 HIGH**, no new R15 finding;
[JSON](/volume/pt-dev/qjiu/rsi_context/artifacts/rsi-core-v1/r15-preflight/bandit-integration.json).
An earlier full run had one loaded-suite race in a process-cleanup test:
its 0.1-second timeout expired before the test child wrote its PID file.
Raising that test timeout to 1 second preserved the timeout cleanup path;
its 13 focused tests and the full rerun passed. The 15 jail skips are due to
the host's unchanged non-root-owned `/usr/bin` trust failure; one live
researcher smoke skipped without an endpoint. Independent code review and a
14-call fully frozen-path fake run found no remaining paid-runner blocker.

Next, freeze a **separate** KEP live launch and budget, then test real
full-source solvability and both model-invoked controls. Seek additional
independent source projects and reject shortcut-prone cases before researcher
draws. Researcher effective-update-rate pretesting requires a trusted host
and an unskipped adverse jail suite; there is no measured update rate yet.

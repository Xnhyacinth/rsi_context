# R21 decision after the Iceberg short screens

R19 and R20 are two development fixtures from the same Iceberg source and
Qwen reader. Both were provider-valid, but the reader selected `suppress-row`
on every tested counterfactual. R20's value-mismatch control also failed,
so the observed behavior is broader than missing the constructed file rule.
The long Qwen–Iceberg source screen is closed; these calls are not independent
worlds or evidence of a general model defect. Preserve both negative paid
blocks and do not tune/retry their frozen launches.

## Next bounded reader diagnostic

The highest-value next paid question is whether a **distinct reader family**
can apply the same short rule contrast. The registered Gemma 4 31B tokenizer
snapshot and Siflow catalog visibility are intake evidence only. Before a
Gemma result, freeze a model-specific profile, exact text chat template,
tokenizer manifest, request hashes, local geometry, provider model echo,
stop/usage requirements, and a short exact-answer canary. Use R20's A/B/C/D
semantic facts and private oracle, adapting only the model's registered chat
envelope. A preregistered single block may spend at most **four task calls
plus two canaries, zero auxiliary calls, no retries**. Its first canary and
provider input-token parity must pass before any task dispatch. Record every
attempt and provider token count, including refusal or unknown usage.

If the second family fails the canary or repeats the fixed suppress response,
retire the paid Iceberg row-scan route. If A/B/C all pass, conclude only that
the simplified short decision is feasible for that reader. A new long-source
registration, no-reread control, source-free/identity controls and separate
review would still precede a long experiment. No short result qualifies an
independent parent or the full four-clause rule.

## Benchmark construction priority

In parallel, scout independent upstream project lineages for B and C task
families. Each candidate needs authentic source pins, a reader-visible
longitudinal evidence dependency, a private action oracle, real project
receipts/actions, source-free and answer-changing controls, measured model
usage, and a demonstrated nontrivial baseline. Count sample size by
independent projects, never by repeated calls or variants of Iceberg. The
model-driven action chain and matched fixed/experience-only/compute controls
remain necessary for the benchmark's strategy-improvement claim.

The researcher effective-update-rate pretest stays separate. The current host
fails the unchanged jail trust check at UID-1000-owned `/usr/bin` and
`/usr/lib`; it has no live brokered worker adapter. Run the adverse jail suite
on a trusted root-owned runtime before scheduling researcher draws, then
freeze the draw denominator, model profile, task/feedback identities, and
per-draw target/auxiliary budget. Report valid submissions, audited and
exercised policies, and task success as separate quantities.

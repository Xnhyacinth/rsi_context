# R14 KEP-753 resource-order task intake, 2026-09-26

Status: **one new constructed development C card in the existing KEP-753
lineage; zero newly qualified independent parents**. No model, Kubernetes
cluster, Siflow API, or GPU was used. The result is an offline source and
evaluator check, not a reader-difficulty or deployment measurement.

## Source identity and scope

The isolated R14 worktree began clean from `main@7cade654`. The upstream
`kubernetes-enhancements-kep753` checkout was detached and clean at
`13e8bb54ff7b1777d97c0f7f3cc9691c67414d4a`, the registry revision. The
builder checks these registered SHA-256 values before constructing a world:

| File | SHA-256 |
| --- | --- |
| `keps/sig-node/753-sidecar-containers/README.md` | `ba9ef6b591cb626023c6e8a0c77cbc3f3cd2ede98d6ab3ff086e41ebef0cdf21` |
| `keps/sig-node/753-sidecar-containers/kep.yaml` | `c991d242d163e88f933172239fa9e712351604448985a6fa302b0010b70cd2d3` |
| `LICENSE` | `b40930bbcf80744c86c46a12bc9da056641d722716c378f5659b9e555ef833e1` |

The decisive README span is lines **761–798**, bytes **36872–38230**
(half-open), SHA-256
`335a67c7a8a77ce39322a2a0b575f68ac4c8bbaeeb665777d837878e852dc33b`.
Those lines give the proposed effective resource-request calculation: for each
regular init container, add the requests of sidecars with earlier indices;
take the maximum over those init uses and concurrent sidecars plus regular
containers. Pod overhead is handled separately. The card asks for an estimate
**under this pinned KEP proposal**, not an assertion about a live scheduler.

## Distinct decision and private oracle

R4 asks whether a continuously running helper can support Job completion and
then satisfy a readiness amendment. R14 asks whether a Pod fits a 1000m CPU
request limit after its init-container order changes. This is a distinct
decision using a different KEP proposition, but both cards belong to the same
KEP-753 source lineage and cannot count as separate independent parents.

The first constructed request orders native sidecar S (300m), regular init A
(800m), then regular init B (500m), with a regular application (400m), zero
overhead, and 1000m capacity. The KEP formula gives
`max(300+800, 300+500, 300+400) = 1100m`, so the private legal plan is
`hold-at-1000m`. In session 2, an amendment moves A before S; the formula
gives `max(800, 300+500, 300+400) = 800m`, so the private legal plan becomes
`admit-at-1000m`. The KEP's own simpler upper-bound formula would give 1100m
even after that reorder; the source's ordered-prefix term matters.

Session 1 presents the **full pinned README** before the project request and
requires a current `resource-review` receipt to finalize `resource_assessment`.
Only bounded carry and persistent project state cross the reset. Session 2
requires the earlier finalized record **as of session entry**, advances only
`resource-review` to revision 2, and requires a fresh receipt to finalize
`resource_reassessment`. Both plan choices receive a passing procedural
receipt, so the verifier cannot reveal the private CPU answer; the final
commit precondition checks the plan. No CPU scheduling command is executed.

The UTF-8 constructed request is 695 bytes, SHA-256
`9fc1271856509a0db74e1893a74fa6a965e38c4ce6d640468f6ac0b00903b391`;
the amendment is 650 bytes, SHA-256
`979d1bc19950aec013ef0b81e2a664f1fd19b493314e00e907a264a8cff4ce0c`.
Canonical first and second `LifecycleInstance` JSON SHA-256 values are
`4565339a3636212ede2dd7fd3ef9e4f4d961e613c939a0f6c2a6010e3e96042a`
and `542cae04f4b34a356bf321771bc988590bb238dd49afad7250c69cd57100a99e`.
These identify development material, not a sealed or frozen gate.

## Offline checks and limits

The deterministic test hook reads the formula from the source stage and carries
only a small formula flag across the actual session reset. It completes both
records with plans and current receipts. Withholding the source makes its
conservative formula choose `hold-at-1000m` after the reorder; the second
receipt still passes, but the evaluator rejects the final plan. Tests also
check removal of just the indexed formula with unrelated text left intact,
stability under an unrelated source note, the naive formula's mismatch,
absent prior finalization, stale evidence, and altered source bytes. Together
with the existing R4 Kubernetes parent tests, **17 focused tests passed**;
the new builder had **100% measured branch coverage**, scoped Ruff and strict
mypy passed, and Bandit reported no findings. These are wiring and arithmetic
controls. They do not prove a language model needs the source or would find the
indexed rule.

Before panel admission, independently adjudicate the request against the KEP,
run a fixed question-general model policy, a genuinely model-invoked source-free
arm and source-target intervention, measure exact prompt geometry and all
calls, and obtain complete-project outcomes from at least two reader families.
Any model-known formula, source-free "hold then admit" heuristic, or feedback
search is a live difficulty risk. Neither this card nor R4 currently qualifies
the KEP lineage as an independent benchmark parent.

Focused reproduction:

```bash
RSICONTEXT_K8S_SOURCE_ROOT=/volume/pt-dev/qjiu/rsi_context_external/data/kubernetes-enhancements-kep753 \
  uv run --frozen --no-sync pytest -q tests/test_k8s_r14.py
uv run --frozen --no-sync ruff check src/rsicontext/lifecycle/material_k8s_r14.py tests/test_k8s_r14.py
uv run --frozen --no-sync mypy --strict src/rsicontext/lifecycle/material_k8s_r14.py tests/test_k8s_r14.py
uv run --frozen --no-sync bandit -q src/rsicontext/lifecycle/material_k8s_r14.py
```

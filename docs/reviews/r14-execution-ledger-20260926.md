# R14 independent-parent and broker continuation

Status: **R14 development slice verified**. Four isolated worktrees started clean at
`main@7cade65484896fce5185a8ec554d939bdae11697`; each used
`uv sync --extra dev --frozen --link-mode copy`. The common `uv.lock` SHA256 is
`5b24847780908a3481e8b6757480531c165ff87517bc1d48acf250baa6cc9352`
and registry SHA256 is
`c071cbfc5c33f69b01abb8b0c58033590711482adbad66bb0c261bc5ef6b67e2`.
The original main checkout's `logs/` remains untouched.

| Worktree | Ownership | Evidence target |
| --- | --- | --- |
| `r14-pep` | PEP 1 source-dependent B intake | Pinned source spans, visible/project/oracle distinction, source-free shortcut audit; admit a development card only with a defensible action, otherwise reject |
| `r14-kep` | KEP-753 C parent intake | Pinned source and constructed rollout split, multi-session action/receipt chain, causal controls; admit a development card only if adjudicated |
| `r14-broker` | Candidate exercise integration | Exact audited `policy/seed.py` staged in unchanged jail, typed broker B/C exercise and usage accounting; current host must fail before candidate execution |
| `r14-integration` | Experiment contract and merge | Reconcile scientific/security review, run full gates, report effective evidence and limits, merge/push main and clean branches |

The qualified independent-parent count is still zero. Scripted tests may
validate wiring, not model difficulty. The current host jail refuses
`/usr/bin` as a non-root-owned ancestor; no researcher-authored code or paid
researcher call may cross that boundary. A fixed benchmark-owned reader
control is separate from researcher execution and needs its own frozen
materials, profile, canaries, provider usage, and bounded stop rules.

The local credential loader documented by earlier runs is
`/volume/pt-dev/qjiu/wynckeliao-env/ops/env/local-env.sh`. A presence-only
check found `SIFLOW_API_KEY`; that file does not set `SIFLOW_BASE_URL`.
No credential value was printed or copied into this project.

## Result and claim boundary

The PEP 621/639 historical license-format B card and KEP-753 resource-order
two-session B card were added as inspected **development** material. The PEP
pair produces opposite private legal plans under two authentic documents from
one PEP lineage. A scripted identity-only witness also passes both arms after
the rule content is removed, using visible PEP numbers/URLs; this blocks a
paid source-dependence claim until model-invoked identity and all-equivalent
rule controls are run. The KEP calculation changes from 1100m (hold) to 800m
(admit) after a constructed order amendment. It has no failed execution and
recovery, so cross-review correctly reclassified it from the planned C intake
to a longitudinal B card. Deleting one formula string leaves equivalent
upstream guidance elsewhere; that test is only scripted wiring evidence.
These add two candidate source lineages, **zero qualified independent
parents** and zero model-difficulty measurements.

The new evaluator-only broker exercise interface requires exact candidate,
task, initial-environment, jail-manifest and interpreter identities. It stages
every session's audited snapshot before any launch, uses the unchanged jail
and typed broker, requires an explicit evaluator decision function, separates
task outcome from admission validity, records target usage, and refuses all
auxiliary delegation. A fresh test on this host stopped at the unchanged
`/usr/bin` trust check before launch. Offline protocol peers verify ordering
and accounting but do not prove actual candidate isolation. `live_ready` is
always false; no researcher draw, Siflow call, GPU use, or measured
effective-update rate occurred in R14. The historical R4 40-call B/C cap
was measured on different development worlds and is **not** a budget for
the new PEP/KEP cards.

Cross-review caught the KEP task-family error and residual rule cue and
verified their correction. A separate PEP review identified the identity
shortcut; the new control reproduces it. Initial Codex branch review found a
P2: the broker interface inherited legacy award/calibration decisions for
new task families. It now refuses a missing task-specific evaluator callback
before staging and has a failing-task regression. The final
`codex review --base origin/main` reported no actionable regression.

The final full run used detached, clean PEP, KEP, PG16/17 and OTel1.24/1.43
source checkouts, a test-only Siflow key without a live endpoint, and the
same PopQA corpus (SHA256
`ca74dec945e9c837be4d7739da2194fbb09b6f8d5affa9823d979936c63fc89f`).
`pytest --cov=rsicontext --cov-branch` finished with **1489 passed, 16 skipped,
80.84% branch coverage**, above the 80% floor. Fifteen skips were the
expected host jail-trust refusal and one was the no-endpoint live researcher
smoke. Repository-wide Ruff lint passed; strict mypy found zero issues in
367 source files. All six R14 Python files pass Ruff format. Bandit matched
the unchanged **81 LOW, 2 MEDIUM, 0 HIGH** baseline, with no finding in new
R14 files.

Raw ignored logs live in the main checkout under
`artifacts/rsi-core-v1/r14-gates/`. SHA256:

| Artifact | SHA256 |
| --- | --- |
| `full-pytest.log` | `23cae0b40ef2f21472575b5acea69b20d8bca81fb66814f05b2d3e1b71b425bc` |
| `bandit.json` | `3f9cd639ed2844db48b6eace623554defc77e09ba87c3cacb90237cdada656f7` |
| `codex-review.log` | `8561720ecf1ceda65e9b0121101e0cb0cdc832634b75f14d6a1ebba24979bee4` |

Next experiment: freeze a question-general reader policy and exact rendered
chat geometry, then run model-invoked source-free/identity-only controls and
complete-project outcomes on at least two reader families under new measured
per-card budgets. The brokered researcher pretest additionally needs a host
where the unchanged jail's adverse suite runs without trust skips, strict
researcher and worker canaries, provider-grounded target/auxiliary accounting,
and a predeclared denominator. Gate 2 and live researcher admission remain
closed.
